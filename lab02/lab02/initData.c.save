#include "initData.h"

void initData(){
	/* asks the usesr for a string (char array)
	and saves in global variable */
	/* create an array of int* of size 5 callled intptrArray
	which is a global variable */
	/* allocate space for an int (holds one int) for each int*
	and initialize the value to random int using rand()*/
}

int main(int argc, char* argv[]){
	return 0;
}
