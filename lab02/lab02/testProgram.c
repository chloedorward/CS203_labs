#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

void testPalindrome(){
	/* calls palindrome on whatever the user inputted */
}

void testEquals(){
	/* calls equals with: 
	intptrArray[0], intptrArray[0]
	intptrArray[0], 0
	intptrArray[1], intptrArray[2]
	*/
}

void testFind(){
	/*
	find(*(intptrArray[0]), intptrArray, 5)
	find(*(intptrArray[3]), intptrArray, 5)
	sets intptrArray[2] to 0 (zeroes out the pointer), then...
	find(*(intptrArray[4]), intptrArray, 5), then...
	reallocate space for intptrArray[2] and reinitializes the value
	*/
}

void testEquals2(){
	/*
	equals2(intptrArray, intptrArray, 5)
	equals2(&intptrArray[1], intptrArray, 4)
	*/
}

void testProgram(){
	/* */
}

int main(int argc, char* argv[]){

}
